# 傑斯克的練歌世界 規格需求

## 1. 產品目標

「傑斯克的練歌世界」是一個部署於 GitHub Pages 的單頁練歌工具，讓使用者登入後管理自己的 YouTube 歌曲庫、建立播放列表、播放歌曲、查看歌詞並進行練唱控制。

## 2. 技術架構

- 主檔案：`index.html`
- 前端：HTML、CSS、Vanilla JavaScript
- 後端服務：Supabase Auth 與 Supabase Database
- 外部播放：YouTube IFrame API
- AI 找歌：Gemini API 搭配 Google Search grounding
- 部署方式：GitHub Pages
- 不需建置流程；直接開啟 `index.html` 或由靜態站台服務即可執行。

## 3. 使用者與權限

- 使用者需以 Email / Password 登入後才能使用歌曲庫與播放列表。
- 支援註冊、登入、登出與忘記密碼。
- 歌曲資料與播放列表需以 `currentUser.id` 綁定使用者。
- 寫入 Supabase 時需遵守 RLS，只能操作自己的歌曲與播放列表。

## 4. 主要畫面

- `歌曲列表`：顯示「我的專屬歌曲庫」，可排序、批量刪除、AI 找新歌、開啟公共資料庫。
- `看歌詞`：顯示目前播放歌曲的歌詞，支援一般文字與 LRC 時間軸歌詞。
- `新增與編輯`：新增或編輯歌曲，並管理歌曲拖曳排序。
- `公共歌曲資料庫`：顯示所有使用者新增過的歌曲，可加入自己的歌曲庫。
- `YouTube 播放畫面`：嵌入 YouTube 影片播放器。
- `播放列表`：右側固定區塊，可加入歌曲、拖曳排序、移除、儲存與清空。

## 5. 歌曲庫需求

- 歌曲欄位包含 `title`、`singer`、`ytId`、`lyrics`、`sort_order`、`user_id`。
- 新增歌曲時，`title` 與 YouTube 連結必填，`singer` 與 `lyrics` 可選填。
- YouTube 連結需解析為 11 碼 `ytId`，無效時不得寫入。
- 編輯歌曲後，需同步更新播放列表中的同一首歌。
- 歌曲可依歌曲名稱筆劃、歌手筆劃排序，並可儲存排序至 Supabase。
- 支援勾選多首歌曲後批量刪除。

## 6. 播放列表需求

- 使用者可將歌曲庫歌曲加入播放列表。
- 播放列表項目需可拖曳排序。
- 播放列表可儲存至 Supabase 的 `saved_playlist`。
- 使用者登入後，若有已儲存播放列表，需自動載入。
- 當歌曲庫資料更新時，播放列表需同步歌曲名稱、歌手、YouTube ID 與歌詞。

## 7. YouTube 播放需求

- 使用 YouTube IFrame API 播放歌曲。
- 支援播放 / 暫停、上一首、下一首、音量調整、快轉 5 秒、倒退 5 秒、全螢幕。
- 支援進度條顯示與拖曳跳轉。
- 支援單曲循環。
- 支援 Media Session API，讓手機控制中心可顯示歌曲資訊並操作播放。
- 背景播放時需盡量維持播放狀態，避免系統自動暫停。

## 8. YouTube 損壞歌曲處理

- 當 YouTube IFrame API 回報 `onError`，或歌曲的 `ytId` 無效時，視為該歌曲已損壞。
- 已損壞歌曲需在前端暫存狀態中標記，不直接修改 Supabase 原始歌名。
- 已損壞歌曲的顯示名稱需在歌名右側加上 `(已損壞)`。
- 標記需同步顯示在「我的專屬歌曲庫」、歌曲排序管理清單、播放列表與目前歌詞標題。
- 播放中遇到損壞歌曲時，需自動跳過並播放下一首未標記損壞的歌曲。
- 若播放列表內沒有其他可播放歌曲，需停止播放並提示使用者。
- 已標記損壞的歌曲再次被點擊播放時，需直接跳過，不重複嘗試載入。

## 9. 歌詞需求

- 支援一般純文字歌詞。
- 支援 LRC 格式時間軸歌詞。
- 播放中若歌詞含時間軸，需依 YouTube 目前播放秒數同步高亮。
- 切換歌曲時需更新歌詞標題與內容。

## 10. AI 找歌需求

- AI 找歌需根據現有歌曲庫避免推薦重複歌曲。
- AI 回傳需解析為歌曲資料，並補齊歌詞與真實 YouTube 連結。
- 無效 YouTube 連結需略過，不得寫入歌曲庫。
- 寫入前需維持或修正現有歌曲排序。
- 失敗時需以 toast 或狀態文字提示使用者。

## 11. 資料表需求

### `songs`

- `id`
- `title`
- `singer`
- `ytId`
- `lyrics`
- `sort_order`
- `user_id`

### `saved_playlist`

- `ytId`
- `title`
- `singer`
- `lyrics`
- `sort_order`
- `user_id`

## 12. 非功能需求

- 不新增前端套件或建置工具。
- 保持單檔 HTML 架構。
- UI 需支援桌機與手機尺寸。
- 錯誤與操作結果需以 alert、toast 或畫面狀態提示使用者。
- 不得在規格文件中記錄 API key、密碼或其他 secret。
